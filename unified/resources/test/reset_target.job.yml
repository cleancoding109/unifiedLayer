resources:
  jobs:
    reset_target_job:
      name: "[${bundle.target}] Reset Target Table"
      tasks:
        - task_key: reset_table
          notebook_task:
            notebook_path: ../../data_setup/reset_target.py
            source: WORKSPACE


