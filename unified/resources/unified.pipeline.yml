# The main pipeline for unified
resources:
  pipelines:
    unified_pipeline:
      name: unified_pipeline
      catalog: workspace
      schema: unified_${bundle.target}
      serverless: true
      libraries:
        - notebook:
            path: ../src/pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
